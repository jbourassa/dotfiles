export PATH=$(brew --prefix php)/bin:/usr/local/bin:/usr/local/sbin:$PATH:$HOME/pear/bin
export NODE_PATH=/usr/local/lib/node_modules:$NODE_PATH

# Let RVM kick in
[[ -s $HOME/.rvm/scripts/rvm ]] && source $HOME/.rvm/scripts/rvm
